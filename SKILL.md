---
name: bianque
description: |
  扁鹊 bianque：按「正确性→安全→性能→错误处理→可读性→测试」六层清单逐条审查代码，输出分级结论（🔴/🟠/🟡/💡）与修复建议。
  触发：代码审查、code review、review 一下、帮我 review、审查这段代码、看看这段代码有什么问题、PR review、帮我看看这个 diff。
  不触发：直接改代码/修 bug（→ 按用户要求直接修改，本技能不介入）；领域术语与架构评审（→ domain-modeling）；评估技能质量（→ darwin-skill）。
---

# 扁鹊 Bianque — 分层代码审查

**核心心智**：审查不是「我觉得」，是「逐条对照清单、每一条都能机械判断」。产出物是带严重级别的问题列表 + 可执行的修复建议，不是散文式感想。

## 1. 审查流程（固定顺序）

1. **拿输入**：完整 diff 或代码片段 + 变更意图（PR 描述/需求）。意图缺失 → 🔴 停下问用户，不脑补。
2. **定领域**：先分类本次代码的领域（见下方领域表），再审查——通用六层清单 + 该领域的失败模式清单叠加使用。领域不清 → 按通用六层审，并在报告标注「领域未定」。
3. **定范围**：只审本次变更，不顺手重写无关代码。
4. **跑六层清单 + 领域清单**（六层见 §3，领域表见上），每层逐条判断，命中的记下「文件:行号 + 级别 + 问题 + 建议」。
5. **定级**：按下方严重级别表给每条定级。
6. **出报告**：按「🔴 → 🟠 → 🟡 → 💡」排序输出，每条一句话问题 + 一句话建议。无问题的层直接写「无」。

### 领域失败模式表（domain-aware，先定领域再叠加）

| 领域 | 识别信号 | 叠加审查重点 |
|---|---|---|
| Web 后端 API | 路由/handler/controller、HTTP 状态码 | 认证与鉴权边界、幂等性（重复请求）、分页上限、输入校验缺失 |
| 数据处理/ETL | 批处理、转换管道、定时任务 | 空批次/部分失败重试幂等、内存溢出、时区与编码、脏数据静默吞 |
| 前端交互 | 组件/事件/状态管理 | 竞态（旧请求覆盖新结果）、卸载后 setState、可访问性、XSS 转义遗漏 |
| 并发/分布式 | 锁/队列/多线程/消息 | 死锁与饥饿、消息重复消费、部分失败一致性、超时与重试放大 |
| 存储/数据库 | ORM/迁移/查询 | N+1 查询、迁移回滚路径、索引缺失、事务边界、锁粒度 |
| 安全敏感 | 鉴权/加密/审计代码 | 时序攻击、弱随机数、密钥轮换、旁路信息泄漏 |
| 算法/数值 | 数值计算、几何、排序 | 浮点精度、除零、溢出、边界收敛条件 |

## 2. 严重级别（定级标准）

| 级别 | 判定（满足其一即定级） |
|---|---|
| 🔴 blocker | 会导致错误结果/崩溃/数据丢失/安全漏洞；或不 merge 无法上线 |
| 🟠 major | 边界条件会出错；明显性能问题；逻辑分支写反 |
| 🟡 minor | 风格不一致、命名误导、可读性差、重复代码 |
| 💡 nit | 可选优化、更地道的写法，不影响正确性 |

## 3. 六层清单

### 3.1 正确性（先审，最高优先）
- 循环边界：off-by-one？空集合/单元素会怎样？
- 空值：函数参数、返回值可能为 null/undefined/空字符串的地方，是否有处理？
- 类型：隐式类型转换、==/===、数字溢出/精度（浮点比较禁 `==`，用 `abs(a-b) < epsilon`）
- 条件：`>`/`>=`、`&&`/`||` 优先级、switch 是否漏 break/default
- 并发/异步：共享状态有无竞态？async 是否缺 await？错误是否被吞？

### 3.2 安全
- 注入：SQL 是否参数化？命令是否拼接外部输入？HTML 是否转义（XSS）？
- 敏感信息：密码/key/token 是否硬编码、是否进日志？
- 权限：是否有越权访问路径（改别人的数据不校验归属）？

**9 条可机械检测的危险模式**（命中 → 初筛标记，按下方「判断关键」定级，不是命中即 🔴）：

| # | 触发特征 | 风险 | 安全替代 |
|---|---|---|---|
| 1 | `.github/workflows/*.yml` 里 `run:` 直接拼 `${{ github.event.* }}` | 工作流命令注入 | 先赋 `env:` 再用 `$VAR` |
| 2 | `child_process.exec` / `execSync` | 命令注入 | `execFile` 传参数数组 |
| 3 | `new Function(...)` | 代码注入 | 改用其他设计模式 |
| 4 | `eval(...)` | 代码注入/RCE | `JSON.parse` / 对象映射表 / mathjs |
| 5 | React `dangerouslySetInnerHTML` | XSS | `DOMPurify.sanitize()` 包裹 |
| 6 | `document.write(...)` | XSS | `createElement` + `textContent` |
| 7 | `.innerHTML = ...` | XSS | `textContent` 或 `DOMPurify` |
| 8 | Python `pickle.loads`（不可信来源） | 反序列化 RCE | 换 JSON 等安全格式 |
| 9 | `os.system` / `from os import system` | 命令注入 | `subprocess.run([...], shell=False)` |

判断关键：**不可控输入 + 危险函数 = 高危**。模式匹配只是初筛，命中后看输入来源定级：输入不可控 → 确认 🔴；输入来自常量/本进程内部且已校验 → 降级并在报告注明理由。

### 3.3 性能
- 循环内是否有 O(n²) 嵌套、循环内发请求/查库/重复计算？
- 大集合是否无脑全量加载、缺分页/懒加载？
- 高频路径是否有不必要的对象创建/字符串拼接？

### 3.4 错误处理
- 每个可能失败的操作（IO/网络/解析/外部调用）有无错误分支？还是只写了 happy path？
- catch 是否吞异常（空 catch）？错误信息是否含上下文？
- 资源是否在 finally 释放（连接/文件句柄/锁）？

### 3.5 可读性 & 命名
- 命名是否与行为一致（叫 `getX` 却有副作用；`flag`/`data`/`tmp` 无意义名）？
- 函数是否过长（>50 行）、嵌套是否 >3 层、魔法数字是否无命名常量？
- 注释是否在解释「为什么」而非「是什么」？

### 3.6 测试
- 新逻辑是否配测试？是否覆盖失败分支而不只是 happy path？
- 测试是否独立（顺序无关、无共享可变状态）、是否真断言而非只跑不验？

## 4. 失败模式

- 意图缺失 → 不猜，🔴 停下要 PR 描述/需求。
- diff 太大（>400 行）→ 拆成多轮，每轮一个模块，避免漏审。
- 清单某条「拿不准是否算问题」→ 定级取高一级（宁可过审不可漏报），标注「待确认」。**仅限真正拿不准的项：已确认低危的项不得机械升级**（否则误报墙钝化 🔴 敏感性）。
- 用户只要「能不能上线」不想要全量报告 → 只给 🔴/🟠 + 一句话结论。

## 5. 检查点（🔴 停下问用户）

- 变更意图缺失或与代码对不上 → 停下要意图。
- 发现 🔴 blocker 且修复方案不唯一 → 列方案让用户选，不擅自定。
- 审查范围不清（是否要顺带审相邻旧代码）→ 停下确认范围。

## 6. 不要做

- 不修代码：审查只报告，不直接改（用户明确说「发现问题直接改」才改）。
- 不审风格工具能自动做的事（缩进/import 顺序）——交给 formatter/linter，清单只留人判断的项。
- 不输出散文式感想，不写「整体不错」空话；无问题就写「无」。
- 不以「我熟悉这项目」为由跳过 3.1 正确性层。
- 不把「能力」（静态分析工具、lint 集成）当本技能职责——那是插件/MCP 的事。

## 7. 诚实边界

- 本技能给清单与定级规则，不含具体语言/框架的领域知识（如 Python 特有坑需另查）。
- 无运行时数据、无法执行的纯静态审查，性能判断只能靠「明显反模式」，无法测出真实瓶颈。
- 安全审查是尽力而为，不替代渗透测试/SAST 工具。
